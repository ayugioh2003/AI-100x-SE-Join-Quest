{
  "name": "order-discount-module",
  "version": "1.0.0",
  "description": "E-commerce order pricing promotions module",
  "main": "index.js",
  "scripts": {
    "test": "jest",
    "test:cucumber": "cucumber-js",
    "test:cucumber:verbose": "cucumber-js --format progress --format summary --format json:cucumber-report.json",
    "test:cucumber:details": "node show-cucumber-report.js",
    "build": "tsc",
    "dev": "ts-node-dev --respawn --transpile-only src/index.ts"
  },
  "devDependencies": {
    "@cucumber/cucumber": "^10.0.0",
    "@types/chai": "^5.2.2",
    "@types/jest": "^29.5.0",
    "@types/node": "^20.0.0",
    "chai": "^4.3.0",
    "jest": "^29.5.0",
    "ts-jest": "^29.1.0",
    "ts-node": "^10.9.0",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.0.0"
  }
}
